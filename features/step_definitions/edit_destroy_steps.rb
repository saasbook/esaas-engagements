Given /^(?:|I )am logged in as a "(.*)"$/ do |type_user|
    #pending
    steps %Q{
        When PENDING:
    }
end

Then /^I should see "(.*)" has button "(.*)"$/ do |username, sid|
    #pending
    steps %Q{
        When PENDING:
    }
end




